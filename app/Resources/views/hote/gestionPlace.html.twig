{% extends "admin/adminTemplate.html.twig" %}


{% block body %}

    <form action="{{ path('valideNomSerie')}}" method="post">
        <input type="text" name="nomSerie"  placeholder="Nom de la nouvelle serie">
        <input type="submit">
    </form>
        
    {% for place in places %}
        <div class="col-sm-6 col-md-2">
            <div class="thumbnail">
                <a href="/edit/picture/serie/{{place.id}}">
                    <p>{{place.adress}}<p>
{#                    <img class="img-thumbnail" src="/web/images/oeuvrePictures/" >
#}                </a>
            </div>
        </div>

    {% endfor %}
{% endblock %}